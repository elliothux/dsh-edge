# @open-compute/dsh-edge 0.12.1

- Prefer vendored patched dependencies when assembling embed Worker aliases.
- Ship TypeScript declarations for the public embed surface (`auth`, `http`, `instance`, `session-store`, `workspace`).
