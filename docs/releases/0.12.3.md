# @open-compute/dsh-edge 0.12.3

- `packClientPlugin` wraps host client code as a DSH ModuleLoader plugin package for `assembleWeb`.
- `finalizeEmbeddedWeb` rewrites scoped plugin paths to URL-safe directories, strips `_headers`, and injects an optional iframe bootstrap script.
