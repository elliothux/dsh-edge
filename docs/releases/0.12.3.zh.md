# @open-compute/dsh-edge 0.12.3

- `packClientPlugin` 将宿主 client 代码包装为可供 `assembleWeb` 使用的 DSH ModuleLoader 插件包。
- `finalizeEmbeddedWeb` 将 scoped 插件路径改写为 URL 安全目录、移除 `_headers`，并可选注入 iframe bootstrap 脚本。
