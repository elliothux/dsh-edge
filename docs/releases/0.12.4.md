# @open-compute/dsh-edge 0.12.4

- Stage every patched standalone package into `vendor/`, including `@deepseek-ai/dsh-client-ui-conversation`, and fail prepack if any patched package is missing.
- Prefer `vendor/` when `assembleWeb` resolves client plugins so embed hosts receive the InputBar `.modes` omit fix instead of an unpatched host copy.
