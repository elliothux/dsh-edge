# @open-compute/dsh-edge 0.12.4

- 将每个已 patch 的 standalone 包装入 `vendor/`（含 `@deepseek-ai/dsh-client-ui-conversation`）；若有缺失则让 prepack 失败。
- `assembleWeb` 解析 client 插件时优先使用 `vendor/`，使 embed 宿主拿到 InputBar `.modes` omit 修复，而不是宿主树里未 patch 的副本。
